import React from "react";
import CartItems from "../Components/CartItems/CartItems";

const Cart = () => {
  return (
    <div>
      {/* Render the CartItems component which displays the items in the cart */}
      <CartItems />
    </div>
  );
};

export default Cart;
